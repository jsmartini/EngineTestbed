net:
  #static ip and port to intranet direct ethernet connection to terminalNetwork host
  target_ip:
  target_port:

data:
  #path to data storing directory
  dir_path:
  #prefix for *.csv files
  fname_prefix:

control_variables:
  #all pressure units are in MPa
  #gpio -> pin number
  max_pressure:
  min_pressure:
  test_duration:
  OF_valve_time_offset:
  gpio:
    tank_pressurize:
      air_valve_gpio:
    tanks_vent:
      fuel_valve_gpio:
      oxidizer_valve_gpio:
    chamber_valves:
      chamber_fuel_valve_gpio:
      chamber_oxidizer_valve_gpio:
    Sensors:
      #!!!!specify pressure transducer functionality!!!!
      pressure1:
      pressure2:
      pressure3:
      pressure4:
      load_cell:



